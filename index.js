// prompt-sync ka use
const prompt = require('prompt-sync')();

const name = prompt('Aapka naam kya hai? ');
console.log(`Namaste, ${name}!`);

const age = prompt('Aapki age kya hai? ');
console.log(`Aapki age hai: ${age}`);
